{
  "name": "workout-app",
  "private": true,
  "version": "1.0.0",
  "description": "Workout / training app. The app itself has no dependencies; these are for the tests only.",
  "type": "module",
  "scripts": {
    "dev": "node dev-server.js",
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.2",
    "jsdom": "^29.1.1",
    "puppeteer-core": "^25.10.0"
  }
}
